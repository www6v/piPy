# my-app workspace

This directory is the agent `cwd`. piPy loads `AGENTS.md` (and related context
files) from here when you do not pass `no_context_files=True`.

- Keep project-specific rules in this file.
- Add `.pi/skills/`, `.pi/prompts/`, or `.pi/extensions/` as needed.
